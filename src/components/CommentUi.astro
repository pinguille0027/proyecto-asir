---
export const prerender = false;
import { map } from 'astro/zod';
import { GET } from '../api/comments.ts'

let response = await GET(Astro)
const comments = await response.json()
console.log(comments)
---
<h1>comentarios</h1>
{Array.isArray(comments) && comments.length > 0 ? comments.map((comment) => <h1>{comment.contenido}</h1>) : <p>sin comentarios</p>}